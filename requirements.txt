# NOTE(Termux/Android): у pydantic-core нет колёс под bionic libc на PyPI,
# голый pip пытается собрать его через Rust и падает. TUR держит готовые
# колёса (сейчас: 2.41.5 под cp314). Версии ниже задублированы с этим фактом:
# pydantic==2.12.4 требует ровно pydantic-core==2.41.5, aiogram согласен
# (pydantic<2.14,>=2.4.1). На обычном Linux индекс TUR просто игнорируется —
# там pip возьмёт manylinux-колёса с PyPI.
--extra-index-url https://termux-user-repository.github.io/pypi/
aiogram>=3.0.0
aiohttp>=3.8.0
aiosqlite>=0.19.0
pydantic==2.12.4
pydantic-core==2.41.5
yt-dlp>=2026.01.01
